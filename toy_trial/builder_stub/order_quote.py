"""Public seam for the source-blind HCR v0.1 toy trial."""


def quote_order(
    subtotal_cents,
    item_count,
    region,
    *,
    member=False,
    expedited=False,
    promo_code=None,
):
    """Return the shipping charge in cents. See RECONSTRUCTION_PACKET.md."""

    raise NotImplementedError("source-blind candidate not implemented")

